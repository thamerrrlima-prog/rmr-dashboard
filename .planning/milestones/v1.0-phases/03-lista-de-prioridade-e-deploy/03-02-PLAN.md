---
phase: 03-lista-de-prioridade-e-deploy
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .streamlit/config.toml
  - requirements.txt
autonomous: true
requirements:
  - UI-01
  - UI-02
  - DEP-01

must_haves:
  truths:
    - "Dashboard exibe dark theme em todas as tabs sem CSS customizado"
    - "requirements.txt na raiz do projeto lista todas as dependências com versões mínimas"
    - ".streamlit/config.toml existe com [theme] base = 'dark'"
    - "Arquivo .streamlit/config.toml é rastreável pelo git (não está no .gitignore)"
  artifacts:
    - path: ".streamlit/config.toml"
      provides: "Configuração de tema dark para Streamlit"
      contains: "[theme]"
    - path: "requirements.txt"
      provides: "Dependências para deploy no Streamlit Cloud"
      min_lines: 5
  key_links:
    - from: ".streamlit/config.toml"
      to: "Streamlit runtime"
      via: "carregado automaticamente pelo Streamlit na inicialização"
      pattern: "base = .dark."
---

<objective>
Criar `.streamlit/config.toml` com dark theme e verificar/atualizar `requirements.txt` para garantir deploy funcional no Streamlit Cloud.

Purpose: A interface usa dark theme consistente em toda a app (UI-01) e o repositório contém todos os arquivos necessários para deploy automático no Streamlit Cloud (DEP-01).
Output: `.streamlit/config.toml` com tema dark + `requirements.txt` correto.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .streamlit/config.toml com dark theme</name>
  <files>.streamlit/config.toml</files>
  <action>
    Criar o diretório `.streamlit/` na raiz do projeto (se não existir) e criar o arquivo `.streamlit/config.toml` com o seguinte conteúdo:

    ```toml
    [theme]
    base = "dark"
    primaryColor = "#1f77b4"
    backgroundColor = "#0e1117"
    secondaryBackgroundColor = "#1a1f2e"
    textColor = "#fafafa"

    [server]
    headless = true
    ```

    Explicação das escolhas (Claude's Discretion — UI-01):
    - `base = "dark"` aplica tema escuro em toda a app sem CSS customizado
    - `primaryColor = "#1f77b4"` é o azul já usado nos gráficos (cor da série "Clientes")
    - `backgroundColor` e `secondaryBackgroundColor` são os tons escuros padrão do Streamlit dark
    - `textColor = "#fafafa"` garante contraste adequado para leitura em notebooks
    - `[server] headless = true` é necessário para Streamlit Cloud (suprime o aviso de email)

    IMPORTANTE: Não criar `.streamlit/.gitignore` nem adicionar config.toml ao .gitignore — o arquivo DEVE ser commitado para o deploy funcionar.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import os, sys
# Verificar que arquivo existe
path = '.streamlit/config.toml'
assert os.path.exists(path), f'{path} não encontrado'
with open(path) as f:
    content = f.read()
# Verificar seção [theme]
assert '[theme]' in content, '[theme] não encontrado em config.toml'
assert 'base = \"dark\"' in content, 'base = dark não encontrado'
# Verificar que não está no .gitignore
gitignore_path = '.gitignore'
if os.path.exists(gitignore_path):
    with open(gitignore_path) as f:
        gi = f.read()
    assert 'config.toml' not in gi, 'config.toml está no .gitignore — remover!'
print('OK — .streamlit/config.toml válido')
"
</automated>
  </verify>
  <done>.streamlit/config.toml existe com [theme] base = "dark", primaryColor e cores de background definidas. Arquivo não está no .gitignore.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar e finalizar requirements.txt para deploy no Streamlit Cloud</name>
  <files>requirements.txt</files>
  <action>
    Ler o `requirements.txt` atual (já existe na raiz com: streamlit, pandas, openpyxl, python-calamine, plotly, pytest).

    Atualizar `requirements.txt` para remover `pytest` (dependência de desenvolvimento, não deve ir para produção no Streamlit Cloud) e garantir que as versões mínimas estão corretas para o ambiente de produção:

    ```
    streamlit>=1.32.0
    pandas>=2.1.0
    openpyxl>=3.1.0
    plotly>=5.0.0
    ```

    Remover `python-calamine>=0.6.0` apenas se confirmar que não é usado em data_loader.py. Se for usado, mantê-lo.
    Remover `pytest>=7.0.0` — não é necessário em produção.

    Verificar data_loader.py para confirmar dependências reais:
    - Se `import calamine` ou `engine="calamine"` aparecer → manter python-calamine
    - Caso contrário → remover

    IMPORTANTE: O arquivo requirements.txt deve conter APENAS dependências de runtime (não dev). O Streamlit Cloud instala exatamente o que está neste arquivo.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
with open('requirements.txt') as f:
    content = f.read()
# pytest não deve estar em produção
assert 'pytest' not in content, 'pytest encontrado em requirements.txt — remover para produção'
# streamlit deve estar presente
assert 'streamlit' in content, 'streamlit não encontrado em requirements.txt'
# pandas deve estar presente
assert 'pandas' in content, 'pandas não encontrado em requirements.txt'
print('OK — requirements.txt válido para deploy')
print('Conteúdo:')
print(content)
"
</automated>
  </verify>
  <done>requirements.txt contém apenas dependências de runtime (sem pytest). streamlit, pandas, openpyxl e plotly presentes com versões mínimas.</done>
</task>

</tasks>

<verification>
cat .streamlit/config.toml confirma [theme] com base = "dark".
cat requirements.txt confirma ausência de pytest e presença de streamlit/pandas/openpyxl/plotly.
</verification>

<success_criteria>
- .streamlit/config.toml criado com dark theme (base = "dark")
- requirements.txt contém apenas dependências de runtime
- Ambos os arquivos prontos para commit e deploy no Streamlit Cloud
- Nenhum arquivo de configuração adicionado ao .gitignore por engano
</success_criteria>

<output>
After completion, create `.planning/phases/03-lista-de-prioridade-e-deploy/03-02-SUMMARY.md`
</output>
