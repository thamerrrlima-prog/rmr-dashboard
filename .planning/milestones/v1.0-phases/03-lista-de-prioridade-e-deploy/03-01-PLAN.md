---
phase: 03-lista-de-prioridade-e-deploy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.py
autonomous: true
requirements:
  - LST-01
  - LST-02
  - LST-03
  - LST-04
  - LST-05

must_haves:
  truths:
    - "Tabs sempre visíveis (5 tabs): Lista | Receita | Painel | Importação | Configuração"
    - "Tab 'Importação' contém o bloco de upload dos dois arquivos e o botão 'Calcular RMR'"
    - "Tabs Lista/Receita/Painel/Configuração mostram st.info orientando o usuário quando dados não estão carregados"
    - "Projeção Estratégica (30/60/90 dias) aparece na Tab Painel (não mais na Tab Receita)"
    - "Tab Receita contém apenas o bloco de receita semanal com filtros"
    - "Tab Lista exibe tabela com ID, Nome, Telefone, GAP, Classificação GAP, Ticket Médio ordenada por GAP ascendente"
    - "Filtros PlayG, Classificação GAP e busca por texto aplicam lógica AND na Tab Lista"
    - "Inelegíveis são excluídos da tabela Lista"
  artifacts:
    - path: "src/app.py"
      provides: "App reestruturada com 5 tabs e Tab Importação"
      contains: "tab_lista, tab_receita, tab_painel, tab_importacao, tab_config"
  key_links:
    - from: "tab_importacao upload block"
      to: "st.session_state['rmr_result']"
      via: "botão Calcular RMR dentro da tab_importacao"
      pattern: "with tab_importacao"
    - from: "tab_painel projeção"
      to: "df_proj calcular_projecao()"
      via: "bloco movido de tab_receita para tab_painel"
      pattern: "calcular_projecao"
---

<objective>
Reestruturar src/app.py para 5 tabs com nova organização: upload vai para tab "Importação", Projeção Estratégica vai para Tab Painel, Tab Lista é adicionada como primeira tab. Tabs são sempre visíveis — antes do upload mostram mensagem orientadora.

Purpose: Cada tab tem um propósito claro. O vendedor que abre pela primeira vez sabe que começa pela Importação. Após importar, vai direto à Lista.
Output: src/app.py reestruturado com 5 tabs funcionando.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app.py
</context>

<interfaces>
<!-- ESTRUTURA ATUAL de src/app.py (resumo): -->
<!--   Linhas 29-32: Inicialização gap_thresholds no session_state -->
<!--   Linhas 34-53: Bloco de upload (dois file_uploader) — MOVER para tab_importacao -->
<!--   Linha 57: Botão "Calcular RMR" — MOVER para tab_importacao -->
<!--   Linhas 59-80: Cálculo e armazenamento no session_state — MOVER para tab_importacao -->
<!--   Linhas 85-95: Exibição condicional (if rmr_result in session_state) — REMOVER o if, tabs sempre visíveis -->
<!--   Linha 95: st.tabs(["Painel", "Receita", "Configuração"]) — SUBSTITUIR por 5 tabs -->
<!--   Linhas 97-205: with tab_painel — Painel analítico. ADICIONAR bloco Projeção Estratégica ao final -->
<!--   Linhas 207-279: with tab_receita — Tab Receita. REMOVER bloco Projeção Estratégica (linhas 257-284) -->
<!--   Linhas 286-329: with tab_config — Configuração dos limiares -->
<!--                                                                    -->
<!-- COLUNAS em result_df: GAP, Faixa_GAP, Nome, Telefone, PlayG, -->
<!--   Segmento_RMR, Monetario, Ritmo. ID pode estar no índice ou como coluna. -->
<!--   Verificar com result_df.columns e result_df.index.name -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reestruturar app.py — 5 tabs, Importação, mover Projeção, adicionar Tab Lista</name>
  <files>src/app.py</files>
  <action>
    Reescrever src/app.py mantendo toda a lógica existente, reorganizando em 5 tabs.

    **ESTRUTURA ALVO** (ordem das seções no arquivo):

    ```
    1. Imports e configuração de página (inalterado)
    2. Inicialização session_state gap_thresholds (inalterado)
    3. st.tabs(5 tabs) — sempre visível, sem condicional externo
    4. with tab_lista:    — Tab Lista (nova)
    5. with tab_receita:  — Tab Receita (apenas receita semanal)
    6. with tab_painel:   — Tab Painel (KPIs + gráficos + Projeção Estratégica)
    7. with tab_importacao: — Tab Importação (upload + calcular)
    8. with tab_config:   — Tab Configuração (inalterado)
    ```

    **Passo 1 — Criar as 5 tabs (remover o st.success e o if condicional externo):**

    Substituir as linhas atuais (~85-95):
    ```python
    # REMOVER: if "rmr_result" in st.session_state:
    #     result_df = st.session_state["rmr_result"]
    #     total_clientes = ...
    #     st.success(...)
    #     tab_painel, tab_receita, tab_config = st.tabs(...)

    # SUBSTITUIR POR:
    tab_lista, tab_receita, tab_painel, tab_importacao, tab_config = st.tabs(
        ["Lista", "Receita", "Painel", "Importação", "Configuração"]
    )
    ```

    **Passo 2 — with tab_importacao: (conteúdo do antigo bloco de upload + cálculo):**

    Mover para dentro de `with tab_importacao:` todo o bloco que estava no topo da app:
    - st.header("Carregar Dados")
    - col1, col2 = st.columns(2) com os dois file_uploader
    - O botão st.button("Calcular RMR", ...) com a lógica de cálculo completa
    - O st.success() com o total de clientes (pode ficar aqui após o cálculo)

    ```python
    with tab_importacao:
        st.header("Carregar Dados")

        col1, col2 = st.columns(2)
        with col1:
            base_bu_file = st.file_uploader(
                "Base B.U.",
                type=["xlsx"],
                key="base_bu",
                help="Arquivo Excel com colunas: ID, Nome, PlayG, Telefone",
            )
        with col2:
            historico_file = st.file_uploader(
                "Histórico de Crédito",
                type=["xlsx"],
                key="historico",
                help="Arquivo Excel com colunas: ID, Tipo, Valor, Data",
            )

        if st.button("Calcular RMR", type="primary", disabled=(base_bu_file is None or historico_file is None)):
            try:
                with st.spinner("Processando dados..."):
                    base_df = load_base_bu(base_bu_file)
                    hist_df = load_historico(historico_file)
                    valid_df = get_valid_transactions(base_df, hist_df)
                    rmr_df = compute_rmr(valid_df)
                    st.session_state["rmr_df"] = rmr_df
                    st.session_state["rmr_result"] = apply_segmentation(
                        rmr_df, st.session_state.get("gap_thresholds", DEFAULT_GAP_THRESHOLDS)
                    )
            except Exception as e:
                import traceback
                st.error(f"Erro ao processar os dados: {e}")
                st.code(traceback.format_exc())
                st.session_state.pop("rmr_result", None)
                st.session_state.pop("rmr_df", None)

        if "rmr_result" in st.session_state:
            result_df = st.session_state["rmr_result"]
            total_clientes = len(result_df)
            inelegiveis = (result_df["Segmento_RMR"] == "Inelegível").sum()
            st.success(f"{total_clientes} clientes no RMR ({inelegiveis} inelegíveis por 1 compra)")
            st.info("Dados carregados. Navegue pelas abas Lista, Receita e Painel.")
    ```

    **Passo 3 — Helper para estado vazio (usar em tabs que precisam de dados):**

    Criar um helper inline (não função separada — uma linha) para o estado vazio:
    ```python
    _sem_dados = "rmr_result" not in st.session_state
    ```
    Usar `_sem_dados` no início de cada tab que precisa de dados.

    **Passo 4 — with tab_lista: (Tab Lista — nova)**

    ```python
    with tab_lista:
        if "rmr_result" not in st.session_state:
            st.info("Importe os dados na aba **Importação** para ver a lista de prioridade.")
        else:
            result_df = st.session_state["rmr_result"]
            elegíveis = result_df[result_df["Segmento_RMR"] != "Inelegível"].copy()

            # Garantir que ID é coluna (pode estar no índice)
            if "ID" not in elegíveis.columns:
                elegíveis = elegíveis.reset_index()
                if elegíveis.columns[0] != "ID":
                    elegíveis = elegíveis.rename(columns={elegíveis.columns[0]: "ID"})

            elegíveis["Ticket Médio"] = elegíveis["Monetario"]

            # Filtros
            f1, f2, f3 = st.columns(3)
            sel_playg = f1.multiselect("PlayG", sorted(elegíveis["PlayG"].dropna().unique()), key="lista_playg")
            sel_faixa = f2.multiselect("Classificação GAP",
                ["Crítico", "Atrasado", "Hora de Comprar", "Em Breve", "Folgado"],
                key="lista_faixa")
            busca = f3.text_input("Buscar nome ou ID", key="lista_busca", placeholder="Digite nome ou ID...")

            # Aplicar filtros (lógica AND)
            df_lista = elegíveis.copy()
            if sel_playg:
                df_lista = df_lista[df_lista["PlayG"].isin(sel_playg)]
            if sel_faixa:
                df_lista = df_lista[df_lista["Faixa_GAP"].isin(sel_faixa)]
            if busca.strip():
                termo = busca.strip().lower()
                mask_nome = df_lista["Nome"].str.lower().str.contains(termo, na=False)
                mask_id = df_lista["ID"].astype(str).str.lower().str.contains(termo, na=False)
                df_lista = df_lista[mask_nome | mask_id]

            # Selecionar e ordenar colunas
            COLUNAS_LISTA = ["ID", "Nome", "Telefone", "GAP", "Faixa_GAP", "Ticket Médio"]
            colunas_existentes = [c for c in COLUNAS_LISTA if c in df_lista.columns]
            df_exibir = (
                df_lista[colunas_existentes]
                .rename(columns={"Faixa_GAP": "Classificação GAP"})
                .sort_values("GAP")
                .reset_index(drop=True)
            )

            st.caption(
                f"{len(df_exibir)} clientes encontrados — ordenados por urgência (GAP ascendente). "
                "Clique no cabeçalho da coluna para reordenar."
            )
            if len(df_exibir) > 0:
                st.dataframe(df_exibir, use_container_width=True, hide_index=True)
            else:
                st.info("Nenhum cliente encontrado com os filtros atuais.")
    ```

    **Passo 5 — with tab_receita: (apenas Receita Semanal)**

    Manter APENAS o bloco de receita semanal (linhas 207-255 do app.py original).
    REMOVER o bloco "Projeção Estratégica" (linhas ~257-284) desta tab.
    Adicionar guard de dados:

    ```python
    with tab_receita:
        if "rmr_result" not in st.session_state:
            st.info("Importe os dados na aba **Importação** para ver a receita.")
        else:
            # [TODO: colar aqui o bloco de Receita Semanal existente (linhas 207-255 de app.py)]
            # NÃO incluir o bloco de Projeção Estratégica — ele vai para tab_painel
    ```

    **Passo 6 — with tab_painel: (KPIs + gráficos + Projeção Estratégica)**

    Manter todo o conteúdo atual da tab_painel (KPI cards, segmentos RMR, histograma GAP).
    ADICIONAR ao final (depois do gráfico de GAP) o bloco de Projeção Estratégica que foi removido da tab_receita:

    ```python
    with tab_painel:
        if "rmr_result" not in st.session_state:
            st.info("Importe os dados na aba **Importação** para ver o painel.")
        else:
            # [TODO: colar aqui todo o conteúdo atual de tab_painel]
            # ...KPI cards, segmentos, histograma GAP...

            st.divider()
            st.subheader("Projeção Estratégica")
            # [TODO: colar aqui o bloco de Projeção Estratégica de tab_receita (linhas ~257-284)]
    ```

    **Passo 7 — with tab_config: (inalterado, adicionar guard)**

    ```python
    with tab_config:
        if "rmr_result" not in st.session_state:
            st.info("Importe os dados na aba **Importação** para configurar os limiares.")
        else:
            # [TODO: colar aqui o conteúdo atual de tab_config — inalterado]
    ```

    **IMPORTANTE:**
    - Não duplicar lógica — cada bloco deve aparecer exatamente uma vez
    - Não criar funções novas — manter estilo flat do app.py
    - Não adicionar botão de exportar
    - Manter todas as variáveis de estado existentes (gap_thresholds, rmr_df, rmr_result)
    - O st.rerun() em tab_config deve continuar funcionando (a tab Configuração ainda tem acesso ao rmr_df via session_state)
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast
with open('src/app.py') as f:
    src = f.read()

# 1. Verificar 5 tabs na ordem correta
assert 'tab_lista, tab_receita, tab_painel, tab_importacao, tab_config' in src, \
    'Ordem das 5 tabs incorreta — esperado: Lista, Receita, Painel, Importação, Configuração'

# 2. Verificar que todos os blocos with existem
for tab in ['tab_lista', 'tab_receita', 'tab_painel', 'tab_importacao', 'tab_config']:
    assert f'with {tab}:' in src, f'Bloco with {tab}: não encontrado'

# 3. Verificar que upload está na tab_importacao (não no topo)
importacao_idx = src.index('with tab_importacao:')
file_uploader_idx = src.index('file_uploader')
assert file_uploader_idx > importacao_idx, \
    'file_uploader deve estar dentro do bloco with tab_importacao:'

# 4. Verificar que Projeção Estratégica está no painel (não em receita)
painel_idx = src.index('with tab_painel:')
receita_idx = src.index('with tab_receita:')
proj_idx = src.index('calcular_projecao')
assert proj_idx > painel_idx, 'calcular_projecao deve estar após with tab_painel:'
# calcular_projecao não deve estar entre receita e painel (se receita vem antes de painel)
if receita_idx < painel_idx:
    assert proj_idx > painel_idx, 'Projeção Estratégica deve estar em tab_painel, não tab_receita'

# 5. Verificar filtros da Lista
assert 'lista_playg' in src, 'filtro PlayG (lista_playg) não encontrado'
assert 'lista_faixa' in src, 'filtro Classificação GAP (lista_faixa) não encontrado'
assert 'lista_busca' in src, 'campo de busca (lista_busca) não encontrado'

# 6. Verificar guard de dados em tabs
assert 'Importe os dados' in src, 'Mensagem de estado vazio não encontrada'

# 7. Parse sintático
ast.parse(src)
print('OK — app.py reestruturado com sucesso (5 tabs, Importação, Projeção em Painel)')
"
</automated>
  </verify>
  <done>
    app.py reestruturado com 5 tabs (Lista | Receita | Painel | Importação | Configuração).
    Tab Importação contém upload + botão Calcular.
    Tabs sem dados mostram st.info orientadora.
    Projeção Estratégica está na Tab Painel.
    Tab Receita tem apenas bloco semanal.
    Tab Lista com filtros PlayG, Faixa GAP, busca texto e tabela ordenada por GAP asc.
    Sintaxe Python válida.
  </done>
</task>

</tasks>

<verification>
python -c "import ast; ast.parse(open('src/app.py').read()); print('syntax OK')" passando.
Grep confirma: 'tab_lista, tab_receita, tab_painel, tab_importacao, tab_config' em app.py.
Grep confirma: 'calcular_projecao' aparece após 'with tab_painel:'.
</verification>

<success_criteria>
- 5 tabs sempre visíveis sem condicional externo
- Tab Importação contém o bloco completo de upload + cálculo
- Tab Lista tem tabela com filtros AND e colunas corretas
- Projeção Estratégica (30/60/90 dias) está em Tab Painel
- Tab Receita tem apenas receita semanal
- Código Python sem erros de sintaxe
</success_criteria>

<output>
After completion, create `.planning/phases/03-lista-de-prioridade-e-deploy/03-01-SUMMARY.md`
</output>
