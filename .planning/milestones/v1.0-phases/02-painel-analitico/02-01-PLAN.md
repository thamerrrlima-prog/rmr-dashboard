---
phase: 02-painel-analitico
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.py
autonomous: true
requirements:
  - CFG-01
  - CFG-02
must_haves:
  truths:
    - "Após cálculo, app.py armazena rmr_df (pré-segmentação) e result_df separados no session_state"
    - "Interface exibe 3 tabs: Painel, Receita, Configuração"
    - "Tab Configuração exibe 4 sliders de limiares de GAP inicializados com DEFAULT_GAP_THRESHOLDS"
    - "Alterar qualquer slider recalcula apply_segmentation com novos limiares sem re-executar compute_rmr"
    - "result_df em session_state é sempre o resultado de apply_segmentation com os limiares atuais dos sliders"
  artifacts:
    - path: "src/app.py"
      provides: "Entrypoint com tabs, session_state split e recálculo de segmentação por limiares"
      contains: "st.tabs"
  key_links:
    - from: "Tab Configuração (sliders)"
      to: "st.session_state['result_df']"
      via: "apply_segmentation(st.session_state['rmr_df'], gap_thresholds)"
      pattern: "apply_segmentation.*session_state"
    - from: "src/app.py"
      to: "src/segmentation.py"
      via: "import DEFAULT_GAP_THRESHOLDS, apply_segmentation"
      pattern: "DEFAULT_GAP_THRESHOLDS"
---

<objective>
Refatorar app.py para separar rmr_df (pré-segmentação) de result_df (pós-segmentação) no session_state, adicionar estrutura de 3 tabs e implementar Tab "Configuração" com sliders que disparam recálculo em tempo real.

Purpose: Fundação obrigatória para os planos 02 e 03 — ambos consomem result_df recalculado com os limiares correntes. Sem essa separação no session_state, o recálculo de GAP por slider é impossível sem re-executar o pipeline pesado.
Output: src/app.py refatorado com tabs, split de session_state e Tab Configuração funcional.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-painel-analitico/02-CONTEXT.md

<interfaces>
<!-- Contratos relevantes do código existente. Executor usa diretamente — sem exploração necessária. -->

De src/segmentation.py:
```python
DEFAULT_GAP_THRESHOLDS = {
    "critico_max": -30,
    "atrasado_max": -8,
    "hora_max": 0,
    "em_breve_max": 7,
}

def apply_segmentation(rmr_df: pd.DataFrame, gap_thresholds=None) -> pd.DataFrame:
    # Adiciona Segmento_RMR e Faixa_GAP ao DataFrame
    # gap_thresholds: dict com critico_max, atrasado_max, hora_max, em_breve_max

def classify_gap_status(gap_value, thresholds=None) -> str:
    # Retorna: "Crítico", "Atrasado", "Hora de Comprar", "Em Breve", "Folgado", "Inelegível"
```

De src/rmr_engine.py:
```python
def compute_rmr(valid_df: pd.DataFrame, reference_date=None) -> pd.DataFrame:
    # Retorna DataFrame indexado por ID com:
    # Nome, PlayG, Telefone, Recencia (int), Monetario (float),
    # Ritmo (float|NaN), GAP (float|NaN),
    # score_R (int|NaN), score_M (int|NaN), score_Ritmo (int|NaN)
```

Estado atual de src/app.py (session_state):
```python
st.session_state["rmr_result"] = result_df  # pós-segmentação, a ser dividido
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separar session_state e estruturar tabs em app.py</name>
  <files>src/app.py</files>
  <action>
Reescrever src/app.py mantendo toda a lógica de upload e cálculo existente, mas com as seguintes mudanças:

**1. Imports adicionais:**
```python
from src.segmentation import apply_segmentation, DEFAULT_GAP_THRESHOLDS
```
(substituir o import atual que já usa apply_segmentation — adicionar DEFAULT_GAP_THRESHOLDS)

**2. Separação no session_state após cálculo:**
No bloco do botão "Calcular RMR", após compute_rmr(), armazenar:
```python
rmr_df = compute_rmr(valid_df)
st.session_state["rmr_df"] = rmr_df          # pré-segmentação (imutável após cálculo)
st.session_state["rmr_result"] = apply_segmentation(
    rmr_df, st.session_state.get("gap_thresholds", DEFAULT_GAP_THRESHOLDS)
)
```
Remover a variável local `result_df = apply_segmentation(rmr_df)` que existia antes.

**3. Inicialização de limiares no session_state:**
Logo antes do bloco de upload (topo do script, após imports), inicializar se ainda não existir:
```python
if "gap_thresholds" not in st.session_state:
    st.session_state["gap_thresholds"] = DEFAULT_GAP_THRESHOLDS.copy()
```

**4. Estrutura de tabs:**
Após o bloco de upload/cálculo existente, quando `"rmr_result" in st.session_state`:
```python
tab_painel, tab_receita, tab_config = st.tabs(["Painel", "Receita", "Configuração"])
```

**5. Tab "Configuração" (implementar agora):**
```python
with tab_config:
    st.subheader("Limiares de GAP")
    st.caption("Alterar os limiares recalcula automaticamente todas as visualizações.")

    thresholds = st.session_state["gap_thresholds"]

    critico_max = st.slider(
        "Crítico: GAP abaixo de (dias)",
        min_value=-120, max_value=-5, step=1,
        value=thresholds["critico_max"],
        help="Clientes com GAP < este valor são classificados como Crítico"
    )
    atrasado_max = st.slider(
        "Atrasado: GAP abaixo de (dias)",
        min_value=-60, max_value=0, step=1,
        value=thresholds["atrasado_max"],
        help="GAP entre crítico_max e este valor = Atrasado"
    )
    hora_max = st.slider(
        "Hora de Comprar: GAP até (dias)",
        min_value=-30, max_value=15, step=1,
        value=thresholds["hora_max"],
        help="GAP entre atrasado_max e este valor = Hora de Comprar"
    )
    em_breve_max = st.slider(
        "Em Breve: GAP até (dias)",
        min_value=1, max_value=30, step=1,
        value=thresholds["em_breve_max"],
        help="GAP entre hora_max e este valor = Em Breve. Acima = Folgado"
    )

    new_thresholds = {
        "critico_max": critico_max,
        "atrasado_max": atrasado_max,
        "hora_max": hora_max,
        "em_breve_max": em_breve_max,
    }

    if new_thresholds != st.session_state["gap_thresholds"]:
        st.session_state["gap_thresholds"] = new_thresholds
        st.session_state["rmr_result"] = apply_segmentation(
            st.session_state["rmr_df"], new_thresholds
        )
        st.rerun()
```

**6. Tab "Painel" e Tab "Receita":**
Colocar placeholders por agora:
```python
with tab_painel:
    st.info("Painel analítico — em construção (Plano 02)")

with tab_receita:
    st.info("Receita projetada — em construção (Plano 03)")
```

**7. Remover o bloco de exibição antigo (dataframe raw):**
O bloco que exibia `st.dataframe(display_df, ...)` diretamente no body principal (fora de tabs) deve ser REMOVIDO — as tabs assumem essa responsabilidade.

Manter o `st.success(f"{total_clientes} clientes no RMR...")` fora das tabs (logo após o cálculo ou antes das tabs), ajustando para ler de `st.session_state["rmr_result"]`.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()
# Verificar presença dos elementos-chave
checks = [
    ('st.tabs' in src, 'tabs criados'),
    ('rmr_df' in src and '\"rmr_df\"' in src, 'session_state[rmr_df] presente'),
    ('gap_thresholds' in src, 'gap_thresholds presente'),
    ('DEFAULT_GAP_THRESHOLDS' in src, 'DEFAULT_GAP_THRESHOLDS importado'),
    ('tab_painel' in src, 'tab_painel presente'),
    ('tab_receita' in src, 'tab_receita presente'),
    ('tab_config' in src, 'tab_config presente'),
    ('st.slider' in src, 'sliders presentes'),
    ('st.rerun' in src, 'st.rerun presente'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed); sys.exit(1)
# Parse syntax check
ast.parse(src)
print('OK — todos os checks passaram')
"
</automated>
  </verify>
  <done>app.py parseia sem erros de sintaxe; session_state armazena rmr_df separado; 3 tabs existem; Tab Configuração tem 4 sliders inicializados com DEFAULT_GAP_THRESHOLDS; alterar slider recalcula apply_segmentation e faz st.rerun()</done>
</task>

</tasks>

<verification>
- `python -c "import ast; ast.parse(open('src/app.py').read()); print('OK')"` — sem erros de sintaxe
- `python -m pytest tests/ -x -q` — todos os testes da fase 1 continuam passando (app.py não tem testes diretos, mas imports não podem quebrar)
- Streamlit inicia sem erro: `python -c "import src.app" 2>&1 | grep -v '^$'` (pode ter warnings de runtime mas não ImportError)
</verification>

<success_criteria>
- app.py salvo com nova estrutura
- session_state["rmr_df"] = DataFrame pré-segmentação (imutável)
- session_state["rmr_result"] = DataFrame pós-segmentação (recalculado pelos sliders)
- session_state["gap_thresholds"] = dict com limiares correntes
- st.tabs(["Painel", "Receita", "Configuração"]) presente
- Tab Configuração com 4 sliders que disparam recálculo via st.rerun()
- Testes da fase 1 não quebram (84 testes)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-painel-analitico/02-01-SUMMARY.md` seguindo o template em @/Users/thamer.../.claude/get-shit-done/templates/summary.md
</output>
