---
phase: 01-motor-rmr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data_loader.py
  - tests/test_data_loader.py
  - requirements.txt
autonomous: true
requirements: [DAD-01, DAD-02, DAD-03, DAD-04, DAD-05]

must_haves:
  truths:
    - "Upload dos dois .xlsx é aceito sem erros"
    - "Transações com tipo 'Troca de Crédito' ou 'Bônus/Desconto' são excluídas"
    - "Transações com valor <= R$0 são excluídas"
    - "Apenas transações a partir de 01/01/2024 são mantidas"
    - "Clientes de PG1 são excluídos do resultado final"
    - "Cada cliente elegível tem ID, Nome, PlayG, Telefone associados aos seus registros de transação"
  artifacts:
    - path: "src/data_loader.py"
      provides: "Funções load_base_bu(), load_historico(), get_valid_transactions()"
      exports: ["load_base_bu", "load_historico", "get_valid_transactions"]
    - path: "tests/test_data_loader.py"
      provides: "Testes automatizados da camada de ingestão"
    - path: "requirements.txt"
      provides: "Dependências do projeto: streamlit, pandas, openpyxl"
  key_links:
    - from: "src/data_loader.py"
      to: "get_valid_transactions()"
      via: "filtragem encadeada: tipo, valor, data, PlayG"
      pattern: "tipo.*Nova Compra|Pagamento"
---

<objective>
Construir a camada de ingestão de dados: ler os dois arquivos .xlsx, aplicar todos os filtros de validade de transação e retornar um DataFrame limpo pronto para o motor RMR.

Purpose: Base de dados limpa é o pré-requisito absoluto para qualquer cálculo RMR correto. Erros aqui se propagam para todos os segmentos.
Output: `src/data_loader.py` com funções testadas, `requirements.txt`, estrutura de projeto.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estrutura do projeto e data_loader.py</name>
  <files>src/data_loader.py, requirements.txt, tests/__init__.py, tests/test_data_loader.py</files>
  <behavior>
    - load_base_bu(file) retorna DataFrame com colunas [ID, Nome, PlayG, Telefone], excluindo PG1
    - load_historico(file) retorna DataFrame bruto de transações com colunas originais preservadas
    - get_valid_transactions(base_df, hist_df) retorna DataFrame filtrado com:
      - Somente tipos "Nova Compra" ou "Pagamento"
      - Valor > 0 (R$0,00 excluído)
      - Data >= 2026-01-01 (período: 01/01/2024 até hoje — usar pd.Timestamp('2024-01-01'))
      - Somente clientes presentes em base_df (join com Base B.U., excluindo PG1 implicitamente)
    - Test: linha com tipo "Troca de Crédito" é descartada
    - Test: linha com valor 0 é descartada
    - Test: linha com data 2023-12-31 é descartada
    - Test: cliente de PG1 não aparece no resultado (PG1 está ausente da base_bu após load_base_bu)
    - Test: cliente com tipo "Nova Compra", valor 500, data 2024-06-01 é mantido
    - Test: retorno tem colunas mínimas: ID, Nome, PlayG, Telefone + colunas de transação
  </behavior>
  <action>
    Criar estrutura de diretórios: src/ e tests/. Criar requirements.txt com: streamlit>=1.32.0, pandas>=2.1.0, openpyxl>=3.1.0.

    Em src/data_loader.py implementar três funções públicas:

    `load_base_bu(file) -> pd.DataFrame`:
    - Aceita path (str) ou file-like object (BytesIO do Streamlit uploader)
    - Lê .xlsx com pd.read_excel()
    - Mantém somente colunas: ID, Nome, PlayG, Telefone (renomear se necessário, usando strip() nos nomes)
    - Filtra: exclui linhas onde PlayG == "PG1" (ou == 1 — tratar ambos)
    - Retorna DataFrame limpo; levanta ValueError descritivo se colunas obrigatórias ausentes

    `load_historico(file) -> pd.DataFrame`:
    - Aceita path ou file-like
    - Lê .xlsx preservando todas as colunas originais
    - Converte coluna de data para datetime (inferir nome comum: "Data", "Data Transacao", etc. — usar regex case-insensitive para encontrar coluna de data)
    - Retorna DataFrame bruto; levanta ValueError se colunas obrigatórias (ID cliente, tipo, valor, data) ausentes

    `get_valid_transactions(base_df, hist_df) -> pd.DataFrame`:
    - Filtra hist_df: tipo IN ["Nova Compra", "Pagamento"] (case-insensitive via .str.strip().str.title())
    - Filtra hist_df: valor > 0 (coluna de valor numérica; coerce não-numéricos para NaN e dropa)
    - Filtra hist_df: data >= pd.Timestamp("2024-01-01")
    - Faz inner join com base_df pelo ID do cliente — isso exclui automaticamente PG1 e clientes não cadastrados
    - Retorna DataFrame com colunas de base_df + colunas de transação

    Criar tests/test_data_loader.py com fixtures em memória (pd.DataFrame direto, sem arquivos .xlsx reais).
    Usar pytest. Cada comportamento listado em <behavior> deve ter ao menos um test function.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -m pytest tests/test_data_loader.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Todos os testes passam. get_valid_transactions() descarta corretamente transações inválidas por tipo, valor, data e PG1. load_base_bu() e load_historico() aceitam file-like objects.</done>
</task>

<task type="auto">
  <name>Task 2: Validação de colunas e mensagens de erro amigáveis</name>
  <files>src/data_loader.py, tests/test_data_loader.py</files>
  <action>
    Adicionar função `validate_columns(df, required_cols, filename_hint)` em src/data_loader.py:
    - Recebe DataFrame, lista de colunas obrigatórias e hint de nome de arquivo
    - Faz matching case-insensitive com strip(): normaliza nomes antes de comparar
    - Se coluna faltante: levanta ValueError com mensagem clara: "Arquivo '{filename_hint}': colunas obrigatórias não encontradas — esperado {expected}, encontrado {actual}"
    - Renomeia colunas para nomes canônicos (ex.: "id cliente" → "ID", "nome do cliente" → "Nome") retornando df com nomes padronizados

    Definir COLUNAS_BASE_BU e COLUNAS_HISTORICO como constantes no topo do módulo para facilitar manutenção.

    Colunas canônicas do Histórico de Crédito: ID (cliente), Tipo, Valor, Data. Colunas canônicas da Base B.U.: ID, Nome, PlayG, Telefone.

    Adicionar testes: upload de arquivo com coluna renomeada (ex.: "id_cliente") ainda funciona; upload sem coluna obrigatória levanta ValueError.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -m pytest tests/test_data_loader.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>Testes de validação passam. Arquivo com colunas renomeadas é normalizado. Arquivo inválido levanta ValueError com mensagem legível.</done>
</task>

</tasks>

<verification>
```bash
cd "/Users/thamer.../Downloads/Claude Code Project" && python -m pytest tests/test_data_loader.py -v
```
Todos os testes devem passar (0 failures, 0 errors).

```bash
cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "from src.data_loader import load_base_bu, load_historico, get_valid_transactions; print('imports OK')"
```
</verification>

<success_criteria>
- `src/data_loader.py` existe com as três funções públicas exportadas
- `requirements.txt` lista streamlit, pandas e openpyxl com versões mínimas
- Todos os testes em `tests/test_data_loader.py` passam
- Transações inválidas (tipo errado, valor zero, data anterior a 2024, PG1) são descartadas
- Colunas com nomes variantes são normalizadas para nomes canônicos
</success_criteria>

<output>
After completion, create `.planning/phases/01-motor-rmr/01-01-SUMMARY.md`
</output>
