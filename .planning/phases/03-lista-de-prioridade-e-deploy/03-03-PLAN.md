---
phase: 03-lista-de-prioridade-e-deploy
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified: []
autonomous: false
requirements:
  - UI-02

must_haves:
  truths:
    - "Tabs sempre visíveis (sem dados mostram mensagem orientadora)"
    - "Ordem das tabs: Lista | Receita | Painel | Importação | Configuração"
    - "Tab Lista abre por padrão e mostra tabela sem scroll horizontal em 1366px"
    - "Tab Painel contém Projeção Estratégica (30/60/90 dias)"
    - "Tab Importação contém upload + botão Calcular RMR"
    - "Dark theme aplica em todas as tabs consistentemente"
    - "Filtros PlayG, Classificação GAP e busca funcionam em ambiente real"
  artifacts:
    - path: ".streamlit/config.toml"
      provides: "Dark theme ativo"
      contains: "base = \"dark\""
    - path: "src/app.py"
      provides: "App 5 tabs reestruturada"
      contains: "tab_lista, tab_receita, tab_painel, tab_importacao, tab_config"
  key_links:
    - from: "vendedor no navegador"
      to: "tab Lista"
      via: "primeira tab visível ao abrir o dashboard"
      pattern: "Lista.*Receita.*Painel.*Importação.*Configuração"
---

<objective>
Verificação visual e funcional do dashboard completo: dark theme em todas as tabs, tab Lista funcionando com filtros, layout responsivo sem scroll horizontal em notebook.

Purpose: Confirmar que UI-02 (layout responsivo) está atendido e que a experiência visual está adequada para uso pelos vendedores antes do deploy.
Output: Aprovação humana para prosseguir com deploy no Streamlit Cloud.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-lista-de-prioridade-e-deploy/03-01-SUMMARY.md
@.planning/phases/03-lista-de-prioridade-e-deploy/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Iniciar app localmente para verificação</name>
  <files></files>
  <action>
    Executar o dashboard localmente para que o usuário possa verificar visualmente:

    ```bash
    cd "/Users/thamer.../Downloads/Claude Code Project"
    streamlit run src/app.py
    ```

    O comando abrirá o browser em http://localhost:8501.

    Se streamlit não estiver instalado no PATH, tentar:
    ```bash
    python -m streamlit run src/app.py
    ```

    Não encerrar o processo — deixar rodando para o checkpoint seguinte.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import subprocess, time, urllib.request, sys
proc = subprocess.Popen(['python', '-m', 'streamlit', 'run', 'src/app.py', '--server.headless=true', '--server.port=8502'], stdout=subprocess.PIPE, stderr=subprocess.PIPE)
time.sleep(5)
try:
    resp = urllib.request.urlopen('http://localhost:8502', timeout=5)
    print('OK — Streamlit respondendo na porta 8502')
except Exception as e:
    print(f'AVISO — Streamlit pode estar iniciando ainda: {e}')
finally:
    proc.terminate()
"
</automated>
  </verify>
  <done>Streamlit inicia sem erros de importação ou de sintaxe. App acessível em localhost:8501.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação visual — dark theme, tab Lista e layout responsivo</name>
  <what-built>
    Dashboard completo com 5 tabs reestruturadas:
    - Ordem: Lista | Receita | Painel | Importação | Configuração
    - Tab Importação: upload dos dois arquivos + botão Calcular RMR
    - Tab Lista: tabela de prioridade com filtros PlayG, Classificação GAP e busca
    - Tab Painel: KPIs + gráficos + Projeção Estratégica (30/60/90 dias)
    - Tab Receita: apenas receita semanal
    - Dark theme via .streamlit/config.toml
    - Tabs sem dados mostram mensagem orientadora
  </what-built>
  <how-to-verify>
    1. Abrir http://localhost:8501 no navegador
    2. Confirmar dark theme: fundo escuro, texto claro em todas as tabs
    3. Confirmar ordem das tabs: Lista | Receita | Painel | Importação | Configuração
    4. Confirmar que tabs Lista/Receita/Painel mostram mensagem "Importe os dados..." antes do upload
    5. Ir para tab Importação, fazer upload dos dois arquivos .xlsx e clicar "Calcular RMR"
    6. Na tab Lista:
       a. Confirmar tabela com colunas: ID, Nome, Telefone, GAP, Classificação GAP, Ticket Médio
       b. Confirmar ordenação padrão GAP ascendente (valores mais negativos no topo)
       c. Testar filtro PlayG: selecionar um PG e verificar que tabela restringe
       d. Testar filtro Classificação GAP: selecionar "Crítico" e verificar linhas
       e. Testar busca: digitar parte de um nome e confirmar resultado case-insensitive
       f. Confirmar que não há scroll horizontal na tabela (colunas cabem na tela)
    7. Na tab Painel: confirmar que Projeção Estratégica (30/60/90 dias) aparece abaixo dos gráficos
    8. Na tab Receita: confirmar que mostra apenas receita semanal (sem Projeção Estratégica)
    6. Navegar pelas outras tabs (Painel, Receita, Configuração) e confirmar que dark theme se mantém
    7. Reduzir janela para ~1366px de largura (viewport de notebook) e confirmar que não há scroll horizontal
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se tudo está correto, ou descreva os problemas encontrados para correção.
  </resume-signal>
  <action>Nenhuma ação automatizada — verificação visual realizada pelo usuário seguindo os passos em how-to-verify.</action>
  <verify>
    <automated>MISSING — verificação é exclusivamente visual/interativa; não há comando automatizado equivalente.</automated>
  </verify>
  <done>Usuário confirmou dark theme em todas as tabs, tab Lista funcional com filtros, e layout sem scroll horizontal em viewport de notebook.</done>
</task>

</tasks>

<verification>
Verificação visual confirmada pelo usuário via checkpoint.
</verification>

<success_criteria>
- Dark theme visível em todas as tabs
- Tab Lista é a primeira tab e funciona com os três filtros
- Layout sem scroll horizontal em 1366px (notebook padrão)
- Usuário aprova para prosseguir com deploy
</success_criteria>

<output>
After completion, create `.planning/phases/03-lista-de-prioridade-e-deploy/03-03-SUMMARY.md`

Then proceed to deploy: push branch to GitHub and connect to Streamlit Cloud at https://share.streamlit.io — select repo, branch main, entry file src/app.py. No secrets needed (app não usa variáveis de ambiente externas).
</output>
