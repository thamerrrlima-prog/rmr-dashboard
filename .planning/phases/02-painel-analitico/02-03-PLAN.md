---
phase: 02-painel-analitico
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app.py
autonomous: true
requirements:
  - REV-01
  - REV-02
  - VIZ-03
must_haves:
  truths:
    - "Tab Receita exibe bloco de receita semanal: soma do ticket médio dos clientes com GAP entre -5 e +5 dias"
    - "Tab Receita exibe projeção estratégica separada: receita projetada para 30, 60 e 90 dias"
    - "Projeção por cliente calculada como ticket_medio × floor(período / ritmo)"
    - "Valores monetários formatados em R$ com separadores de milhar"
  artifacts:
    - path: "src/app.py"
      provides: "Tab Receita com receita semanal e projeção 30/60/90 dias"
      contains: "tab_receita"
  key_links:
    - from: "Tab Receita (receita semanal)"
      to: "elegíveis com GAP entre -5 e +5"
      via: "elegíveis[(elegíveis['GAP'] >= -5) & (elegíveis['GAP'] <= 5)]"
      pattern: "GAP.*-5.*5|semanal"
    - from: "Tab Receita (projeção)"
      to: "ticket_medio e Ritmo por cliente"
      via: "Monetario / num_compras como ticket médio; período / Ritmo como compras esperadas"
      pattern: "Ritmo.*periodo|projecao|30.*60.*90"
---

<objective>
Implementar a Tab "Receita" em app.py com dois blocos distintos: receita semanal (clientes com GAP entre -5 e +5) e projeção estratégica (receita esperada para 30, 60 e 90 dias calculada por cliente).

Purpose: Dá ao vendedor visibilidade de receita imediata e futura — "quanto vou faturar essa semana se ligar para todo mundo" e "qual minha projeção para o próximo mês".
Output: Tab Receita com dois blocos visuais e cálculos corretos de receita projetada.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-painel-analitico/02-CONTEXT.md
@.planning/phases/02-painel-analitico/02-01-SUMMARY.md

<interfaces>
<!-- Colunas disponíveis em result_df (output de apply_segmentation(compute_rmr(...))): -->
<!-- Nome, PlayG, Telefone, Recencia (int), Monetario (float), Ritmo (float|NaN), GAP (float|NaN), -->
<!-- score_R, score_M, score_Ritmo, Segmento_RMR (str), Faixa_GAP (str) -->
<!-- Índice: ID -->

<!-- Nota: "Monetario" = soma de transações válidas do cliente no período -->
<!-- Ticket médio = Monetario / número de compras do cliente -->
<!-- PORÉM: num_compras não está em result_df — usar Monetario como proxy de ticket médio  -->
<!-- OU calcular a partir de Ritmo: se Ritmo = total_dias / (n_compras - 1) e Recencia + GAP = Ritmo -->
<!-- Abordagem mais simples e defensável: ticket_medio = Monetario (total no período, sem divisão) -->
<!-- Projeção: para cada cliente elegível, expected_compras = floor(periodo_dias / ritmo) -->
<!--           receita_projetada_cliente = ticket_medio × expected_compras -->
<!-- Para receita semanal: usar Monetario diretamente como estimativa (por cliente com GAP entre -5 e +5) -->

<!-- session_state após plano 01: -->
<!-- st.session_state["rmr_result"] → result_df completo (pós-segmentação) -->
<!-- st.session_state["rmr_df"] → DataFrame pré-segmentação (tem mesmas colunas menos Segmento/Faixa) -->

<!-- Estrutura de tabs já criada no plano 01: -->
<!-- tab_painel, tab_receita, tab_config = st.tabs(["Painel", "Receita", "Configuração"]) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco de receita semanal na Tab Receita</name>
  <files>src/app.py</files>
  <action>
Substituir o placeholder `st.info("Receita projetada — em construção")` na Tab Receita pelo conteúdo real.

**Dentro de `with tab_receita:`**, implementar:

**1. Carregar dados:**
```python
result_df = st.session_state["rmr_result"]
elegíveis = result_df[result_df["Segmento_RMR"] != "Inelegível"].copy()
```

**2. Cálculo do ticket médio por cliente:**
O DataFrame não tem coluna de num_compras diretamente. Usar Monetario como ticket médio total do cliente (estimativa conservadora — é a receita gerada no período, não por compra). Documentar isso no código com um comentário.

```python
# Ticket médio = Monetario total do cliente no período (estimativa conservadora)
elegíveis["ticket_medio"] = elegíveis["Monetario"]
```

**3. Bloco de receita semanal:**
```python
st.subheader("Receita Semanal")
st.caption("Clientes com GAP entre -5 e +5 dias — janela imediata de compra.")

semanal_mask = (elegíveis["GAP"] >= -5) & (elegíveis["GAP"] <= 5)
df_semanal = elegíveis[semanal_mask]

receita_semanal = df_semanal["ticket_medio"].sum()
n_clientes_semanal = len(df_semanal)

col1, col2 = st.columns(2)
col1.metric("Receita Potencial (semana)", f"R$ {receita_semanal:,.0f}".replace(",", "."))
col2.metric("Clientes na Janela", f"{n_clientes_semanal}")

if n_clientes_semanal > 0:
    st.dataframe(
        df_semanal[["Nome", "PlayG", "GAP", "Faixa_GAP", "ticket_medio"]]
        .sort_values("GAP")
        .rename(columns={"ticket_medio": "Ticket Médio (R$)"})
        .reset_index(),
        use_container_width=True,
        hide_index=True,
    )
else:
    st.info("Nenhum cliente na janela de -5 a +5 dias com os limiares atuais.")
```
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()
checks = [
    ('Receita Semanal' in src, 'titulo receita semanal'),
    ('ticket_medio' in src, 'ticket_medio calculado'),
    ('GAP.*-5' in src or '-5' in src, 'filtro GAP -5'),
    ('Receita Potencial' in src, 'metrica receita potencial'),
    ('Clientes na Janela' in src, 'metrica clientes janela'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed); sys.exit(1)
ast.parse(src)
print('OK')
"
</automated>
  </verify>
  <done>Tab Receita exibe bloco semanal com 2 métricas (receita potencial + n clientes) e tabela de clientes na janela de ±5 dias ordenada por GAP</done>
</task>

<task type="auto">
  <name>Task 2: Projeção estratégica 30/60/90 dias na Tab Receita</name>
  <files>src/app.py</files>
  <action>
Ainda dentro de `with tab_receita:`, após o bloco semanal, adicionar bloco de projeção estratégica.

```python
st.divider()
st.subheader("Projeção Estratégica")
st.caption(
    "Receita esperada por período — calculada por cliente como "
    "Ticket Médio × compras esperadas (período ÷ Ritmo)."
)

# Apenas clientes com Ritmo definido (elegíveis já exclui Inelegível, mas Ritmo pode ser NaN em edge cases)
df_proj = elegíveis[elegíveis["Ritmo"].notna() & (elegíveis["Ritmo"] > 0)].copy()

def calcular_projecao(df: pd.DataFrame, periodo_dias: int) -> float:
    """Soma receita projetada: ticket_medio × floor(periodo_dias / ritmo) por cliente."""
    compras_esperadas = (periodo_dias / df["Ritmo"]).apply(lambda x: max(int(x), 0))
    return (df["ticket_medio"] * compras_esperadas).sum()

proj_30 = calcular_projecao(df_proj, 30)
proj_60 = calcular_projecao(df_proj, 60)
proj_90 = calcular_projecao(df_proj, 90)

c1, c2, c3 = st.columns(3)
c1.metric("30 dias", f"R$ {proj_30:,.0f}".replace(",", "."))
c2.metric("60 dias", f"R$ {proj_60:,.0f}".replace(",", "."))
c3.metric("90 dias", f"R$ {proj_90:,.0f}".replace(",", "."))

st.caption(
    f"Base de cálculo: {len(df_proj)} clientes com Ritmo definido "
    f"(excluídos clientes com 1 compra)."
)
```

Após os dois blocos da Tab Receita, rodar os testes para garantir que nada quebrou:
`python -m pytest tests/ -x -q`
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()
checks = [
    ('Projeção Estratégica' in src or 'Proje' in src, 'titulo projecao'),
    ('calcular_projecao' in src, 'funcao calcular_projecao'),
    ('30' in src and '60' in src and '90' in src, 'periodos 30/60/90'),
    ('proj_30' in src, 'variavel proj_30'),
    ('proj_60' in src, 'variavel proj_60'),
    ('proj_90' in src, 'variavel proj_90'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed); sys.exit(1)
ast.parse(src)
print('OK — syntax and content checks passed')
" && python -m pytest tests/ -x -q 2>&1 | tail -5
</automated>
  </verify>
  <done>Tab Receita exibe bloco de projeção com 3 métricas (30/60/90 dias) calculadas como ticket_medio × floor(periodo/Ritmo) por cliente; testes da fase 1 continuam passando</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` — 84 testes passando
- `python -c "import ast; ast.parse(open('src/app.py').read()); print('OK')"` — sem erros de sintaxe
- Tab Receita: bloco semanal com métricas + tabela de clientes na janela ±5 dias
- Tab Receita: bloco projeção com 3 métricas de 30/60/90 dias
- Valores monetários formatados como R$ com ponto como separador de milhar
</verification>

<success_criteria>
- Bloco semanal: receita potencial e n clientes da janela GAP ∈ [-5, +5] com tabela ordenada por GAP
- Bloco projeção: 3 st.metric (30/60/90 dias) calculados corretamente
- calcular_projecao() usa floor(periodo/Ritmo) como compras esperadas
- Formatação monetária consistente (R$ com ponto como milhar)
- 84 testes da fase 1 passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-painel-analitico/02-03-SUMMARY.md` seguindo o template em @/Users/thamer.../.claude/get-shit-done/templates/summary.md
</output>
