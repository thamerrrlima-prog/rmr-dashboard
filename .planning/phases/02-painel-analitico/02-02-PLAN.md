---
phase: 02-painel-analitico
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app.py
autonomous: true
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-04
must_haves:
  truths:
    - "Tab Painel exibe 4 KPI cards: total de clientes no RMR, % com GAP negativo, receita em risco, ritmo médio da base"
    - "Tab Painel exibe lista de segmentos RMR com contagem de clientes por segmento"
    - "Tab Painel exibe histograma de distribuição de GAP pelas 5 faixas (barras com quantidade de clientes)"
    - "Todos os valores refletem result_df corrente (recalculam quando limiares mudam)"
  artifacts:
    - path: "src/app.py"
      provides: "Tab Painel com KPIs, lista de segmentos e histograma de GAP"
      contains: "tab_painel"
  key_links:
    - from: "Tab Painel (KPIs)"
      to: "st.session_state['result_df']"
      via: "result_df = st.session_state['rmr_result']"
      pattern: "session_state.*rmr_result"
    - from: "Histograma GAP"
      to: "Faixa_GAP"
      via: "value_counts na coluna Faixa_GAP"
      pattern: "Faixa_GAP.*value_counts|groupby.*Faixa_GAP"
---

<objective>
Implementar a Tab "Painel" em app.py com 4 KPI cards, lista de segmentos RMR e histograma de GAP por faixas. Todos os elementos lêem de st.session_state["rmr_result"] — já recalculado pelo plano 01 quando limiares mudam.

Purpose: Principal visão analítica para o vendedor — mostra o estado da base em um relance (quem está em risco, qual o ritmo médio, como está distribuído por segmento).
Output: Tab Painel funcional com st.metric cards, tabela de segmentos e gráfico de barras de GAP.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-painel-analitico/02-CONTEXT.md
@.planning/phases/02-painel-analitico/02-01-SUMMARY.md

<interfaces>
<!-- Colunas disponíveis em result_df (output de apply_segmentation(compute_rmr(...))): -->
<!-- Nome, PlayG, Telefone, Recencia (int), Monetario (float), Ritmo (float|NaN), GAP (float|NaN), -->
<!-- score_R, score_M, score_Ritmo, Segmento_RMR (str), Faixa_GAP (str) -->
<!-- Índice: ID -->

<!-- Segmentos RMR possíveis: -->
<!-- "Campeões", "Não Pode Perder", "Leais", "Novos Clientes", -->
<!-- "Potenciais Leais", "Em Risco", "Hibernando", "Precisam Atenção", "Inelegível" -->

<!-- Faixas GAP possíveis (em ordem de urgência): -->
<!-- "Crítico", "Atrasado", "Hora de Comprar", "Em Breve", "Folgado", "Inelegível" -->

<!-- session_state após plano 01: -->
<!-- st.session_state["rmr_result"] → result_df completo -->
<!-- st.session_state["rmr_df"] → DataFrame pré-segmentação -->
<!-- st.session_state["gap_thresholds"] → dict com limiares correntes -->

<!-- Estrutura de tabs já criada no plano 01: -->
<!-- tab_painel, tab_receita, tab_config = st.tabs(["Painel", "Receita", "Configuração"]) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPI cards e lista de segmentos na Tab Painel</name>
  <files>src/app.py</files>
  <action>
Substituir o placeholder `st.info("Painel analítico — em construção")` na Tab Painel pelo conteúdo real.

**Dentro de `with tab_painel:`**, implementar em ordem:

**1. Carregar result_df do session_state:**
```python
result_df = st.session_state["rmr_result"]
elegíveis = result_df[result_df["Segmento_RMR"] != "Inelegível"]
```

**2. Calcular métricas para os KPI cards:**
```python
total_rmr = len(elegíveis)
pct_gap_negativo = (elegíveis["GAP"] < 0).sum() / len(elegíveis) * 100 if len(elegíveis) > 0 else 0
receita_risco = elegíveis[elegíveis["GAP"] < 0]["Monetario"].sum()
ritmo_medio = elegíveis["Ritmo"].dropna().mean()
```

**3. Exibir 4 KPI cards com st.metric (em 4 colunas):**
```python
c1, c2, c3, c4 = st.columns(4)
c1.metric("Clientes no RMR", f"{total_rmr:,}")
c2.metric("GAP Negativo", f"{pct_gap_negativo:.1f}%")
c3.metric("Receita em Risco", f"R$ {receita_risco:,.0f}".replace(",", "."))
c4.metric("Ritmo Médio", f"{ritmo_medio:.0f} dias" if not pd.isna(ritmo_medio) else "—")
```

**4. Separador e título:**
```python
st.divider()
st.subheader("Distribuição por Segmento RMR")
```

**5. Lista de segmentos (tabela simples):**
Ordenar pela prioridade definida nas decisões do projeto:
```python
ORDEM_SEGMENTOS = [
    "Campeões", "Não Pode Perder", "Leais", "Novos Clientes",
    "Potenciais Leais", "Em Risco", "Hibernando", "Precisam Atenção"
]
seg_counts = (
    elegíveis["Segmento_RMR"]
    .value_counts()
    .reindex(ORDEM_SEGMENTOS, fill_value=0)
    .reset_index()
)
seg_counts.columns = ["Segmento", "Clientes"]
st.dataframe(seg_counts, use_container_width=True, hide_index=True)
```
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()
checks = [
    ('st.metric' in src, 'st.metric presente'),
    ('Clientes no RMR' in src, 'KPI total clientes'),
    ('GAP Negativo' in src, 'KPI GAP negativo'),
    ('Receita em Risco' in src, 'KPI receita risco'),
    ('Ritmo Médio' in src, 'KPI ritmo medio'),
    ('ORDEM_SEGMENTOS' in src, 'ORDEM_SEGMENTOS presente'),
    ('Distribuição por Segmento' in src or 'Distribui' in src, 'titulo segmentos'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed); sys.exit(1)
ast.parse(src)
print('OK')
"
</automated>
  </verify>
  <done>Tab Painel mostra 4 st.metric com valores calculados de elegíveis; lista de segmentos exibe 8 segmentos com contagem, ordenados por prioridade</done>
</task>

<task type="auto">
  <name>Task 2: Histograma de GAP por faixas na Tab Painel</name>
  <files>src/app.py</files>
  <action>
Ainda dentro de `with tab_painel:`, após a tabela de segmentos, adicionar histograma de GAP.

**Adicionar import no topo do arquivo (se ainda não existir):**
```python
import plotly.express as px
```
(Plotly já é dependência do projeto — confirmado em requirements.txt)

**Dentro de `with tab_painel:`, após a lista de segmentos:**

```python
st.divider()
st.subheader("Distribuição de GAP por Faixa")

ORDEM_FAIXAS = ["Crítico", "Atrasado", "Hora de Comprar", "Em Breve", "Folgado"]
CORES_FAIXAS = {
    "Crítico": "#d62728",
    "Atrasado": "#ff7f0e",
    "Hora de Comprar": "#2ca02c",
    "Em Breve": "#1f77b4",
    "Folgado": "#9467bd",
}

gap_counts = (
    elegíveis[elegíveis["Faixa_GAP"].isin(ORDEM_FAIXAS)]["Faixa_GAP"]
    .value_counts()
    .reindex(ORDEM_FAIXAS, fill_value=0)
    .reset_index()
)
gap_counts.columns = ["Faixa", "Clientes"]

fig = px.bar(
    gap_counts,
    x="Faixa",
    y="Clientes",
    color="Faixa",
    color_discrete_map=CORES_FAIXAS,
    labels={"Faixa": "Faixa de GAP", "Clientes": "Quantidade de Clientes"},
    text="Clientes",
)
fig.update_traces(textposition="outside")
fig.update_layout(showlegend=False, plot_bgcolor="rgba(0,0,0,0)")
st.plotly_chart(fig, use_container_width=True)
```

Verificar que `plotly` está em requirements.txt — se não estiver, adicionar `plotly>=5.0.0`.
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()
checks = [
    ('plotly' in src or 'import plotly' in src, 'plotly importado'),
    ('ORDEM_FAIXAS' in src, 'ORDEM_FAIXAS presente'),
    ('px.bar' in src, 'gráfico de barras presente'),
    ('Distribuição de GAP' in src or 'Distribui' in src, 'titulo histograma'),
    ('st.plotly_chart' in src, 'st.plotly_chart presente'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed); sys.exit(1)
ast.parse(src)
print('OK')
" && python -m pytest tests/ -x -q 2>&1 | tail -5
</automated>
  </verify>
  <done>Tab Painel exibe gráfico de barras horizontais com as 5 faixas de GAP coloridas por faixa e com quantidade de clientes; testes da fase 1 continuam passando</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` — 84 testes continuam passando
- `python -c "import ast; ast.parse(open('src/app.py').read()); print('OK')"` — sem erros de sintaxe
- Tab Painel: 4 KPI cards visíveis, lista de 8 segmentos, gráfico de barras de GAP
</verification>

<success_criteria>
- 4 st.metric cards (total, %, receita, ritmo) calculados sobre elegíveis
- Tabela de segmentos com 8 linhas em ordem de prioridade
- Gráfico px.bar com 5 faixas de GAP e cores distintas
- Todos os valores derivam de st.session_state["rmr_result"] — recalculam quando limiares mudam
- 84 testes da fase 1 passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-painel-analitico/02-02-SUMMARY.md` seguindo o template em @/Users/thamer.../.claude/get-shit-done/templates/summary.md
</output>
