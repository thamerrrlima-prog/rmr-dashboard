---
phase: 02-painel-analitico
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.py
autonomous: true
gap_closure: true
requirements:
  - VIZ-02
  - VIZ-04
must_haves:
  truths:
    - "Distribuição por segmento RMR renderiza como barras horizontais (px.bar orientation='h'), não como tabela"
    - "Cada barra de segmento mostra receita total (Monetario.sum()) além da contagem de clientes"
    - "Histograma de GAP por faixas mostra dois valores por faixa: quantidade de clientes e receita (Monetario.sum()), com barras agrupadas"
    - "Todos os valores recalculam quando limiares de GAP mudam (leem de st.session_state['rmr_result'])"
  artifacts:
    - path: "src/app.py"
      provides: "Tab Painel com segmento como px.bar horizontal e histograma de GAP com barmode='group'"
      contains: "orientation='h'"
    - path: "src/app.py"
      provides: "GAP histogram com coluna Receita agregada por Faixa_GAP"
      contains: "barmode='group'"
  key_links:
    - from: "seg_bar (px.bar)"
      to: "elegíveis['Monetario'].groupby('Segmento_RMR')"
      via: "groupby().agg() com colunas Clientes e Receita"
      pattern: "groupby.*Segmento_RMR.*agg|agg.*Clientes.*Receita"
    - from: "gap_bar (px.bar barmode='group')"
      to: "elegíveis['Monetario'].groupby('Faixa_GAP')"
      via: "groupby().agg() com colunas Clientes e Receita por Faixa"
      pattern: "groupby.*Faixa_GAP.*agg|agg.*Clientes.*Receita"
---

<objective>
Fechar as lacunas VIZ-02 e VIZ-04 em src/app.py com edições cirúrgicas na Tab Painel.

VIZ-02: O bloco de segmentos RMR (linha ~126) usa st.dataframe — substituir por px.bar horizontal com colunas Clientes e Receita (Monetario.sum()) por Segmento_RMR.

VIZ-04: O histograma de GAP (linhas ~140-159) mostra apenas contagem de clientes — adicionar coluna Receita por Faixa_GAP e usar barmode='group' para mostrar as duas séries lado a lado.

VIZ-03 (heatmap R x Ritmo): EXCLUÍDO INTENCIONALMENTE — o usuário descartou esta feature explicitamente no CONTEXT.md ("Heatmap R × Ritmo — descartado pelo usuário (nao necessário para v1)") e nas decisões de implementação ("substitui heatmap 5x5 — heatmap nao necessário"). Implementar violaria a decisão do usuário.

Purpose: Completar VIZ-02 e VIZ-04 conforme requisitados — visualização de receita por segmento e por faixa de GAP.
Output: src/app.py atualizado com dois gráficos corrigidos na Tab Painel.
</objective>

<execution_context>
@/Users/thamer.../.claude/get-shit-done/workflows/execute-plan.md
@/Users/thamer.../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-painel-analitico/02-CONTEXT.md
@.planning/phases/02-painel-analitico/02-VERIFICATION.md

<interfaces>
<!-- Estado atual de src/app.py relevante para este plano: -->

<!-- BLOCO VIZ-02 (linhas ~112-126) — a ser substituído: -->
<!--
        st.divider()
        st.subheader("Distribuição por Segmento RMR")

        ORDEM_SEGMENTOS = [
            "Campeões", "Não Pode Perder", "Leais", "Novos Clientes",
            "Potenciais Leais", "Em Risco", "Hibernando", "Precisam Atenção"
        ]
        seg_counts = (
            elegíveis["Segmento_RMR"]
            .value_counts()
            .reindex(ORDEM_SEGMENTOS, fill_value=0)
            .reset_index()
        )
        seg_counts.columns = ["Segmento", "Clientes"]
        st.dataframe(seg_counts, use_container_width=True, hide_index=True)
-->

<!-- BLOCO VIZ-04 (linhas ~128-159) — a ser ampliado: -->
<!--
        st.divider()
        st.subheader("Distribuição de GAP por Faixa")

        ORDEM_FAIXAS = ["Crítico", "Atrasado", "Hora de Comprar", "Em Breve", "Folgado"]
        CORES_FAIXAS = { ... }

        gap_counts = (
            elegíveis[elegíveis["Faixa_GAP"].isin(ORDEM_FAIXAS)]["Faixa_GAP"]
            .value_counts()
            .reindex(ORDEM_FAIXAS, fill_value=0)
            .reset_index()
        )
        gap_counts.columns = ["Faixa", "Clientes"]

        fig = px.bar(gap_counts, x="Faixa", y="Clientes", ...)
        st.plotly_chart(fig, ...)
-->

<!-- Colunas disponíveis em elegíveis: -->
<!-- Nome, PlayG, Telefone, Recencia, Monetario (float — ticket total do período), -->
<!-- Ritmo, GAP, score_R, score_M, score_Ritmo, Segmento_RMR, Faixa_GAP -->

<!-- ORDEM_SEGMENTOS (prioridade de exibição, do mais valioso ao que precisa atenção): -->
<!-- "Campeões", "Não Pode Perder", "Leais", "Novos Clientes", -->
<!-- "Potenciais Leais", "Em Risco", "Hibernando", "Precisam Atenção" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir tabela de segmentos por px.bar horizontal com receita (VIZ-02)</name>
  <files>src/app.py</files>
  <action>
Localizar o bloco "Distribuição por Segmento RMR" na Tab Painel (começa em `st.divider()` antes de `st.subheader("Distribuição por Segmento RMR")`).

Substituir TODO o bloco de construção e renderização do segmento — desde a linha `ORDEM_SEGMENTOS = [` até `st.dataframe(seg_counts, use_container_width=True, hide_index=True)` inclusive — pelo seguinte código:

```python
        ORDEM_SEGMENTOS = [
            "Campeões", "Não Pode Perder", "Leais", "Novos Clientes",
            "Potenciais Leais", "Em Risco", "Hibernando", "Precisam Atenção"
        ]
        seg_data = (
            elegíveis[elegíveis["Segmento_RMR"].isin(ORDEM_SEGMENTOS)]
            .groupby("Segmento_RMR", observed=True)
            .agg(Clientes=("Segmento_RMR", "count"), Receita=("Monetario", "sum"))
            .reindex(ORDEM_SEGMENTOS, fill_value=0)
            .reset_index()
        )
        seg_data.columns = ["Segmento", "Clientes", "Receita"]
        # Inverter ordem para que o gráfico horizontal mostre Campeões no topo
        seg_data = seg_data.iloc[::-1].reset_index(drop=True)

        fig_seg = px.bar(
            seg_data,
            x="Clientes",
            y="Segmento",
            orientation="h",
            text="Clientes",
            hover_data={"Receita": ":,.0f"},
            labels={"Clientes": "Quantidade de Clientes", "Segmento": "Segmento RMR"},
            color_discrete_sequence=["#1f77b4"],
        )
        fig_seg.update_traces(textposition="outside")
        fig_seg.update_layout(
            showlegend=False,
            plot_bgcolor="rgba(0,0,0,0)",
            yaxis={"categoryorder": "array", "categoryarray": seg_data["Segmento"].tolist()},
            margin={"l": 160},
        )
        st.plotly_chart(fig_seg, use_container_width=True)
        st.caption(
            f"Receita total por segmento (ticket médio): "
            + " | ".join(
                f"{row['Segmento']}: R$ {row['Receita']:,.0f}".replace(",", ".")
                for _, row in seg_data.iloc[::-1].iterrows()
                if row["Clientes"] > 0
            )
        )
```

Notas de implementação:
- `groupby(..., observed=True)` evita FutureWarning do pandas em versões >= 2.0
- `hover_data={"Receita": ":,.0f"}` exibe a receita formatada no tooltip ao passar o mouse
- `margin={"l": 160}` reserva espaço adequado para nomes longos de segmentos no eixo Y
- A `st.caption` abaixo do gráfico exibe a receita por segmento de forma legível como texto — complementa o gráfico sem sobrepor
- NÃO remover as constantes `ORDEM_SEGMENTOS` — são usadas localmente neste bloco
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()

checks = [
    ('orientation' in src, 'orientation presente (px.bar horizontal)'),
    (\"orientation='h'\" in src, \"orientation='h' especificado\"),
    ('seg_data' in src, 'seg_data presente (substitui seg_counts)'),
    ('st.dataframe(seg_counts' not in src, 'st.dataframe(seg_counts) removido'),
    ('fig_seg' in src, 'fig_seg definido'),
    ('Receita' in src, 'coluna Receita presente'),
    ('groupby' in src, 'groupby usado para agrupamento'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed)
    sys.exit(1)
ast.parse(src)
print('OK — VIZ-02 verificado')
"
</automated>
  </verify>
  <done>
Gráfico de barras horizontais px.bar substituiu o st.dataframe na seção "Distribuição por Segmento RMR". Cada barra representa um segmento e exibe a quantidade de clientes; o tooltip mostra a receita total (Monetario.sum()) do segmento. Uma caption abaixo lista receita por segmento em texto.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adicionar coluna Receita ao histograma de GAP com barmode='group' (VIZ-04)</name>
  <files>src/app.py</files>
  <action>
Localizar o bloco "Distribuição de GAP por Faixa" na Tab Painel (começa com `st.divider()` antes de `st.subheader("Distribuição de GAP por Faixa")`).

Substituir TODO o bloco de construção do gap_counts e da fig — desde `gap_counts = (` até `st.plotly_chart(fig, use_container_width=True)` inclusive — pelo seguinte código:

```python
        gap_data = (
            elegíveis[elegíveis["Faixa_GAP"].isin(ORDEM_FAIXAS)]
            .groupby("Faixa_GAP", observed=True)
            .agg(Clientes=("Faixa_GAP", "count"), Receita=("Monetario", "sum"))
            .reindex(ORDEM_FAIXAS, fill_value=0)
            .reset_index()
        )
        gap_data.columns = ["Faixa", "Clientes", "Receita"]

        # Formato longo para px.bar com barmode='group'
        gap_long = gap_data.melt(
            id_vars="Faixa",
            value_vars=["Clientes", "Receita"],
            var_name="Métrica",
            value_name="Valor",
        )

        fig_gap = px.bar(
            gap_long,
            x="Faixa",
            y="Valor",
            color="Métrica",
            barmode="group",
            text="Valor",
            category_orders={"Faixa": ORDEM_FAIXAS, "Métrica": ["Clientes", "Receita"]},
            color_discrete_map={"Clientes": "#1f77b4", "Receita": "#2ca02c"},
            labels={"Faixa": "Faixa de GAP", "Valor": "Quantidade / Receita (R$)"},
        )
        fig_gap.update_traces(
            texttemplate="%{y:,.0f}",
            textposition="outside",
        )
        fig_gap.update_layout(
            plot_bgcolor="rgba(0,0,0,0)",
            legend_title_text="",
        )
        st.plotly_chart(fig_gap, use_container_width=True)
```

Notas de implementação:
- O bloco `ORDEM_FAIXAS` e `CORES_FAIXAS` já estão definidos antes deste trecho — não redefinir
- `gap_long` usa `.melt()` para transformar o DataFrame wide em long — formato necessário para px.bar com barmode='group' e duas séries (Clientes e Receita)
- `texttemplate="%{y:,.0f}"` formata os valores nas barras com separador de milhar
- `color_discrete_map` define azul para Clientes e verde para Receita — CORES_FAIXAS foi usado antes para colorir por faixa; aqui a cor distingue a métrica (não a faixa)
- Renomear `gap_counts` → `gap_data` e `fig` → `fig_gap` para evitar colisão de nomes de variável com o bloco original (se o bloco antigo ficou parcialmente no arquivo)
- Verificar que as variáveis `gap_counts` e `fig` do bloco original foram completamente removidas — não devem coexistir com `gap_data` e `fig_gap`
  </action>
  <verify>
    <automated>cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
import ast, sys
with open('src/app.py') as f:
    src = f.read()

checks = [
    ('barmode' in src, \"barmode presente (barras agrupadas)\"),
    (\"barmode='group'\" in src, \"barmode='group' especificado\"),
    ('gap_data' in src, 'gap_data definido (substitui gap_counts)'),
    ('gap_long' in src, 'gap_long (formato melt para duas series)'),
    ('melt' in src, '.melt() presente'),
    ('Receita' in src, 'coluna Receita presente no histograma GAP'),
    ('fig_gap' in src, 'fig_gap definido'),
]
failed = [msg for ok, msg in checks if not ok]
if failed:
    print('FALHOU:', failed)
    sys.exit(1)
ast.parse(src)
print('OK — VIZ-04 verificado')
" && cd "/Users/thamer.../Downloads/Claude Code Project" && python -m pytest tests/ -x -q 2>&1 | tail -5
</automated>
  </verify>
  <done>
Histograma de GAP exibe dois grupos de barras por faixa — Clientes (azul) e Receita (verde) — com barmode='group'. Os 84 testes da fase 1 continuam passando. Sintaxe do app.py validada sem erros.
  </done>
</task>

</tasks>

<verification>
Verificações finais após ambas as tasks:

```bash
# Sintaxe Python válida
cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "import ast; ast.parse(open('src/app.py').read()); print('OK — sem erros de sintaxe')"

# Testes da fase 1 passando
cd "/Users/thamer.../Downloads/Claude Code Project" && python -m pytest tests/ -x -q 2>&1 | tail -5

# Elementos VIZ-02 e VIZ-04 presentes
cd "/Users/thamer.../Downloads/Claude Code Project" && python -c "
src = open('src/app.py').read()
checks = {
    'VIZ-02 orientation=h': \"orientation='h'\" in src,
    'VIZ-02 seg_data groupby': 'seg_data' in src and 'groupby' in src,
    'VIZ-02 st.dataframe removido': 'st.dataframe(seg_counts' not in src,
    'VIZ-04 barmode=group': \"barmode='group'\" in src,
    'VIZ-04 melt': 'melt' in src,
    'VIZ-04 gap_data': 'gap_data' in src,
    'Receita coluna': src.count('Receita') >= 3,
}
failed = [k for k, v in checks.items() if not v]
print('FAILED:', failed) if failed else print('ALL OK')
"
```
</verification>

<success_criteria>
- Tab Painel: "Distribuição por Segmento RMR" renderiza como gráfico de barras horizontais (orientation='h'), não como tabela
- Cada barra de segmento exibe contagem de clientes; tooltip ou caption exibe receita (Monetario.sum()) por segmento
- Tab Painel: "Distribuição de GAP por Faixa" exibe dois grupos por faixa — Clientes e Receita — com barmode='group'
- Sintaxe de src/app.py válida (ast.parse sem exceção)
- 84 testes da fase 1 continuam passando (python -m pytest tests/ -x -q)
- VIZ-03 (heatmap) NÃO implementado — decisão explícita do usuário mantida
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-painel-analitico/02-04-SUMMARY.md` seguindo o template em @/Users/thamer.../.claude/get-shit-done/templates/summary.md
</output>
